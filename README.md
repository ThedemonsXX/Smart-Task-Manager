# Smart-Task-Manager
Simple Task Management web app built with vanilla JavaScript. Create, edit, delete tasks with popup modals, track status (pending/completed), store data in localStorage, format dates (YYYY-MM-DD HH:mm), and search tasks with partial matching. Lightweight CRUD project for DOM practice.
